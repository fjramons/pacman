---
# Named with a "00-" prefix so a plain "*.yaml" glob/directory apply
# applies it first: kubectl doesn't reorder by resource kind, so without
# this the Namespace wouldn't exist yet when deployment.yaml (earlier
# alphabetically otherwise) gets applied.
apiVersion: v1
kind: Namespace
metadata:
  name: pg-sim
